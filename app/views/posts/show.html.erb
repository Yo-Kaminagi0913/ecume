<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1.0">
  <title>Post Detail – écume</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">

  <!-- ヘッダー -->
  <header class="flex items-center justify-between bg-white shadow p-4">
    <!-- 戻るボタン -->
    <a href="javascript:history.back()" class="p-2">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-gray-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
      </svg>
    </a>
    <!-- ロゴ／プレースホルダー -->
    <div class="w-24 h-8 bg-gray-200 rounded"></div>
    <!-- ハンバーガーメニュー -->
    <button class="p-2">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-gray-600" fill="none"
           viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16"/>
      </svg>
    </button>
  </header>

  <!-- メインコンテンツ -->
  <main class="flex-1 p-4 space-y-4">
    <div class="bg-white border border-gray-200 rounded-xl p-4 shadow-sm">
      <div class="flex items-center space-x-3 mb-4">
        <div class="p-2 bg-teal-50 rounded-full">
          <!-- ユーザーアイコン -->
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-teal-200" fill="currentColor"
               viewBox="0 0 24 24">
            <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67
                     0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
          </svg>
        </div>
        <span class="font-semibold text-gray-800"><%= @post.user.name %></span>
        <span class="text-gray-500 text-sm"><%= time_ago_in_words(@post.created_at) %> ago</span>
      </div>
      <p class="text-gray-700 whitespace-pre-wrap"><%= @post.content %></p>
    </div>
  </main>

  <!-- フッター：お気に入りボタンなど -->
  <footer class="bg-white p-4 shadow-inner flex justify-center items-center pb-[env(safe-area-inset-bottom)]">
    <%= render 'favorites/favorite_button', post: @post %>
  </footer>
</body>
</html>

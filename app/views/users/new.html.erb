<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Sign Up – écume</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-teal-50 flex flex-col items-center min-h-screen">

  <!-- アプリ名 -->
  <h1 class="text-white text-4xl font-bold mt-16">écume</h1>

  <!-- サインアップカード -->
  <div class="bg-white rounded-2xl shadow-lg w-full max-w-md mt-8 p-8">
    <!-- 見出し -->
    <h2 class="text-teal-100 text-center text-2xl font-semibold mb-6">Sign Up</h2>

    <!-- フォーム -->
    <%= form_with model: @user, url: signup_path, local: true, data: { turbo: false }, class: "space-y-6" do |f| %>

      <!-- Email -->
      <div>
        <%= f.label :email, class: "block text-gray-700 mb-1" %>
        <%= f.email_field :email,
              placeholder: "Enter email...",
              required: true,
              class: "w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-teal-200" %>
      </div>

      <!-- Password -->
      <div>
        <%= f.label :password, class: "block text-gray-700 mb-1" %>
        <%= f.password_field :password,
              placeholder: "Enter password...",
              required: true,
              class: "w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-teal-200" %>
      </div>

      <!-- Full Name -->
      <div>
        <%= f.label :name, "Full Name", class: "block text-gray-700 mb-1" %>
        <%= f.text_field :name,
              placeholder: "Enter full name...",
              required: true,
              class: "w-full border border-gray-300 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-teal-200" %>
      </div>

      <!-- Submit ボタン -->
      <%= f.submit "SIGN UP",
            class: "w-full bg-gray-300 text-white font-semibold py-3 rounded-lg hover:bg-teal-400 hover:text-white transition" %>
    <% end %>

    <!-- ログインへのリンク -->
    <p class="text-center mt-4">
      <%= link_to "Already have an account?", login_path,
            class: "text-gray-700 uppercase text-sm hover:underline" %>
    </p>
  </div>

</body>
</html>
